# use glob syntax
syntax: glob

*.elc
*.pyc
*~
*.db

# Virtualenv
venv
build

# setuptools
build/*
dist/*

# tests
.coverage
.mypy_cache/
.cache/

# sphinx
docs/_build

# Emacs
eproject.cfg

# Temporary files (vim backups)
*.swp

.idea/

# Log files:
*.log

emissions.csv
vulntrain/config/conf.py
vulnerability/
results/

# Sightings cache: the 24-CVE paper corpus is already tracked; the larger
# scale-validation corpus (hundreds of CVEs) is regenerable via
# scripts/build_large_corpus.py + data/large_corpus.json, so ignore the rest.
data/sightings_cache/

